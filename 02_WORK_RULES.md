# WORK RULES｜Codex作業ルール

このファイルは、つむぎ行政書士事務所LPの制作中にCodexが守る共通ルールです。各作業では最新のユーザー指示を最優先し、正本・正式素材・既存実装を無断で変更しません。

## 1. 制作方針

- AIへすべてを任せず、制作者が構造と理由を理解しながら進める
- 一度に全セクションを実装せず、段階的に進める
- SP版を正本とし、PC版は後から差分を調整する
- 最新のクリエイティブ方針は「写真を主役＋シンプルなアイコンで情報整理」
- 参考サイトをそのまま複製しない
- 実装前に不足情報・矛盾・未決事項を報告する
- 既存機能を壊す可能性がある場合は変更前に確認する
- 法律・行政書士業務について、根拠のない断定表現を追加しない
- 実在の資格番号・住所・登録番号等を捏造しない

## 2. 正本の優先順位

1. 最新のユーザー指示
2. `00_PROJECT_BRIEF.md`
3. `DESIGN_RULES.md`
4. `03_DECISIONS_LOG.md`
5. `IMAGE_ASSETS.md`
6. 参考サイト・参考資料
7. AIによる提案

矛盾がある場合は、上位の情報と最新決定を優先する。判断できない場合は、勝手に確定せずユーザーへ確認する。

## 3. Markdown運用

- `00_PROJECT_BRIEF.md`：企画、ターゲット、CV、構成、サイトテキストの正本
- `01_REFERENCE_NOTES.md`：参考サイトから取り入れる考え方と翻訳方針
- `02_WORK_RULES.md`：作業中に守る共通ルール
- `03_DECISIONS_LOG.md`：決定・変更理由・制作履歴
- `DESIGN_RULES.md`：ブランドとビジュアルの設計ルール
- `IMAGE_ASSETS.md`：正式画像のファイル名・用途・状態の台帳
- `README.md`：プロジェクト全体の案内
- `HANDOFF.md`：実装・デザインの引き継ぎが必要になった段階で作成する。現時点では未作成
- 同じ情報を無意味に複数ファイルへ重複させない
- 正式テキストはユーザー確認なしに修正・要約・言い換えしない
- 未決事項は「未定」と記載し、推測で埋めない

## 4. 実装前のルール

- 実装前に `00_PROJECT_BRIEF.md`、`DESIGN_RULES.md`、`IMAGE_ASSETS.md` を確認する
- セクション構成・ターゲット・コンバージョン・使用素材の整合性を確認する
- HTML / CSS / JavaScriptの変更を依頼されていない作業ではコードを変更しない
- 画像生成・加工・変換を依頼されていない作業では画像を変更しない
- Markdownを書き換える場合は、対象と目的を事前に説明する
- `design/pencil/` は過去のプロジェクト構成として残すが、本LPの制作では使用しない。明示的な指示なしに内容を追加・変更しない

## 5. 素材管理ルール

### 正式素材

- 正式素材は `assets/images/` 配下の用途別フォルダへ格納する
- 正式素材の一覧・用途は `IMAGE_ASSETS.md` を正本とする
- ロゴ・写真・アイコンの内容、ファイル名、拡張子、配置を無断で変更しない
- PNGをJPGへ変換しない。圧縮・切り抜き・背景除去等も明示的な指示なしに行わない
- 画像の置換が必要な場合は、影響するHTML / CSSと合わせて事前に報告する

### 下書き素材

- 修正前、比較用、不採用、正式採用前の候補は `assets/images/_drafts/` で管理する
- `_drafts` はGit管理対象外
- 正式素材と下書き素材を混在させない
- Downloads等にある候補画像を、正式採用の確認なしにプロジェクトへ移動しない

### 命名

- 英小文字、数字、ハイフン、アンダースコアを使用する
- 日本語名・空白・`ChatGPT Image` 形式の名前を正式素材に残さない
- 用途が分かる接頭辞を付ける：`logo_`、`hero_`、`about_`、`office_`、`flow_`、`check-point_`、`reason_`、`contract-status_`
- 既存正式素材のファイル名は、ユーザー指示なしに変更しない

## 6. HTMLルール

- `header` / `main` / `section` / `footer` でランドマークを整理する
- Heroは `h1`
- 各主要セクションは `h2`
- カードやSTEPは `h3`
- `section` と見出しを `aria-labelledby` で関連付ける
- 順番が重要な内容は `ol`
- 通常の箇条書きは `ul`
- FAQは `details` / `summary` を優先する
- フォームは `label` を必須とし、関連入力は `fieldset` / `legend` を検討する
- Skip linkを配置する
- 内容画像には意味のある `alt` を設定する
- 隣接テキストと意味が重複する装飾アイコンは `alt=""` 等を適切に使い分ける
- 見出し階層を崩さない
- 見た目のためだけの不要な `div` を増やしすぎない
- JavaScriptが動かなくても内容を読める構造にする

## 7. CSSルール

- モバイルファースト
- 色・余白・角丸・時間・最大幅をCSS変数で管理する
- 共通コンテナ幅を設け、`max-width` と `margin-inline: auto` で中央配置する
- 画像に `width` / `height` を設定する
- 必要に応じて `aspect-ratio` / `object-fit` を使用する
- GridとFlexboxを役割で使い分ける
- 375px・390pxで横はみ出しを確認する
- 768px直後に無理な2カラムを作らない
- 1024px以上でPCレイアウトを検討する
- `focus-visible` を設定する
- `prefers-reduced-motion` に対応する
- sticky要素とoverflowの関係を確認する
- アンカー移動には `scroll-margin-top` を検討する
- transformや疑似要素が画面幅を超えないよう管理する

## 8. JavaScriptルール

- HTML標準機能で実装できるものはHTMLを優先する
- 見た目の動きはCSSを優先する
- JavaScriptは状態管理・計算・監視に限定する
- 対象要素が存在しない場合にエラーを出さない
- IntersectionObserverを優先する
- scrollイベントには `requestAnimationFrame` を検討する
- JavaScriptが動かなくても内容を読める構造にする
- デバッグコードを本番へ残さない
- `prefers-reduced-motion` を壊さない

## 9. 確認幅

- SP：375px / 390px
- タブレット：768px
- PC：1024px / 1366px / 1440px
- 必要に応じて実機確認する
- SPをPCの縮小版にせず、情報順序・カード配置・導線を再構成する

## 10. Git運用

- ブランチ：`main`
- リモート：https://github.com/Coconattsu0723/tsumugi-lp
- commitは意味のある作業単位で分ける
- commit前に `git status` と差分を確認する
- ユーザーの明示的な指示なしに `git commit` / `git push` を行わない
- 無関係な変更を同じcommitへ混ぜない
- `_drafts`、`.DS_Store`、一時ファイルをcommitしない
- 履歴を書き換える操作や破壊的なGit操作は、明示的な指示なしに行わない

## 11. 作業後の報告項目

1. 変更したファイル
2. 変更したまとまり
3. 使用した構造・タグ・実装理由
4. 共通パーツとページ固有部分
5. SPとPCの違い
6. JavaScriptの役割
7. 確認した画面幅
8. 既存機能への影響
9. 未決事項・確認事項
10. 初心者が覚えるべきポイント
11. 実務で説明する場合の言い方
12. 次の工程

作業内容に該当しない項目は省略可能だが、変更ファイル・検証内容・未決事項・次工程は必ず報告する。

## 12. 禁止事項

- ユーザー確認なしの全面再設計
- 参考サイトのコピー
- 正式テキストの無断変更
- ロゴ・写真・アイコンの無断加工、置換、移動、リネーム
- 正式素材と `_drafts` の混在
- 不要なライブラリ・JavaScriptの追加
- 原因不明のままoverflowで隠すだけの修正
- デバッグ用コードの残置
- SP修正によるPC崩れ、PC修正によるSP崩れ
- 実装内容を説明せずコードだけ提示すること
- ユーザー指示なしのcommit / push
